query PlayListEditorByIdQuery($id: Int!) {
    PlayListEditorById(id: $id) {
        playListEditor {
            id
            tracks {
                id
                title
            }
        }
        errors {
            field
            messages
        }
    }
}
