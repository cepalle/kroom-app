mutation UserAddMusicalPreference($genreId: Int!, $userId: Int!) {
    UserAddMusicalPreference(genreId: $genreId userId: $userId) {
        errors {
            field
            messages
        }
        user {
            musicalPreferences {
                id
                name
            }
        }
    }
}