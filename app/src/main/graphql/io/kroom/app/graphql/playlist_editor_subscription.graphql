subscription PlayListEditorByIdSubscription($id: Int!) {
    PlayListEditorById(id: $id) {
        playListEditor {
            id
            name
        }
        errors {
            field
            messages
        }
    }
}
