mutation UserUpdatePasswordMutation($email: String!, $newPassword: String!) {
    UserUpdatePassword(email: $email, newPassword: $newPassword) {
        errors {
            field
            messages
        }
    }
}
